import { TruncateHoverPipe } from './truncate-hover.pipe';

describe('TruncateHoverPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateHoverPipe();
    expect(pipe).toBeTruthy();
  });
});
